services:
  - type: web
    name: remit-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: cd .. && node dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production