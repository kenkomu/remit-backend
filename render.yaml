services:
  - type: web
    name: remit-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd /opt/render/project/src && npm ci && npm run build
    startCommand: node ../dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production